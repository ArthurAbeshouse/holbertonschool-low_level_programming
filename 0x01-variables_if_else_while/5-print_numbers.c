#include <unistd.h>
#include <stdlib.h>
#include <time.h>
/**
* main - uses "putschar" to print the english alphabet in lowercase
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%d", n);
	return (0);
}
